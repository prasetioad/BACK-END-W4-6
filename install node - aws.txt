54.163.33.33


Install Node.js terbaru:
“curl -sL https://deb.nodesource.com/setup_12.x | sudo -E bash -”
“sudo apt install -y nodejs”

Install PM2: npm install pm2 -g (for running node app)

Setting Security Group untuk Inbound rules sesuai PORT yang diinginkan
